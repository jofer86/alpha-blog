<h1> THIS IS THE INDEX FOOLIO </h1>

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
  </tr>
  <% @article.each do  |article| %>
  <tr>
    <td> <%= article.title %> </td>
    <td> <%= article.description%> </td>
    <td> <%= link_to 'Edit', edit_article_path(article)%> </td>
    <td> <%= link_to 'Show', article_path(article)%> </td> 
    <td> <%= link_to 'Obliterate', article_path(article), method: :delete, data: {confim: "Is You sure homoeby"} %> </td> 
  </tr>
  
  <% end %>
</table>
<%= link_to 'Create new entry', new_article_path%>